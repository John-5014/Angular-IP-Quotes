import { VoteHighlightDirective } from './vote-highlight.directive';

describe('VoteHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new VoteHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
